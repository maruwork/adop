[pytest]
# ADOP test suite. Run from the repo root with: python -m pytest
# conftest.py prepends shared/python/ to sys.path so runtime modules import cleanly.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
