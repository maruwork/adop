# External Tool Adoption Note Template

**When to use**: when deciding whether to adopt an external tool.
**What to replace**: tool name, classification, authority placement, trial state names.
**Do not include**: project-specific current operations, formal rule body, daily execution board.

**What the shared shelf holds**: the note skeleton only.
**What goes on the project side**: current state, actual operation triggers, operator flow, final authority for landing target.

## 1. tool

- name:
- source:
- version / date:
- platform:
- license:
- cost:
- category:
- ai compatibility:
- data flow destination:
- data flow types:
- data flow opt-in:

## 2. classification

- adoption class: `operations integration` / `creation assistance`
- harness layer: `interface` / `mechanism` / `scaling`
- phase: `core build` / `future extension` / `implementation` / `self-improvement loop`

## 3. why this tool

- solved problem:
- why existing repo assets are insufficient:
- why this tool is a better fit than prompt-only operation:

## 4. usage scene

- when to use:
- trigger condition:
- trial executor:
- decision owner:
- who uses it:
- what input it receives:
- what output it produces:

## 5. authority boundary

- authority:
- not authority:
- SSOT writeback target:

## 6. control / verification

- trigger / runbook / hook / queue:
- fail-close:
- escalation:
- verification:
- evaluation gate:
- promote / hold / reject criteria:

## 6.5 trial lifecycle

- trial id:
- current state: `proposed` / `trial-ready` / `in-trial` / `promote` / `hold` / `reject`
- landing target:
- writeback target:
- trial start command:
- trial close command:
- artifact:
- observed effect:
- next action:

## 7. repeatability

- one-off or recurring:
- if recurring, what will be promoted:

## 8. current decision

- adopt now / hold / reject:
- follow-up task:
