# ADOP Standalone Project Note

Status: Active

## Purpose

Record that this directory is the active standalone ADOP project root.

## Current Decision Boundary

- `ADOP` is handled as its own top-level project
- governance concretization lives under `docs/governance/`
- use repo-relative paths when verifying this repository
